# Manuscript errata and formalization clarifications

Source: `paper/manuscript.tex`, SHA-256
`b212a597bf0f76505609fb7fcf569aed8b0681048fcb4e8b2e3e5603b8c95c40`.

This ledger records suspected errors, omissions, and choices needed to make an
informal passage type-correct. It does not amend the paper. Each resolution
must cite a numbered ruling in.

## Paper numbering reference (authoritative)

Recovered from the pinned manuscript and independently re-derived from its
theorem/equation counters, and re-derived independently a second time.
**Cite by `\label` name plus `manuscript.tex` line range.**
Bare numerals drift; several in this repository were wrong until 2026-09-03, and
some still are.

| Label | Number | `manuscript.tex` |
|---|---|---|
| `thm:main` | Theorem 1.1 | `192` |
| `thm:annealed` | Theorem 1.2 | `207` |
| `prop:generator` | Proposition 2.1 | `576` |
| `prop:frequency` | Proposition 2.2 | `644` |
| `lem:formulas` | Lemma 3.1 | `827` |
| `lem:onecoin` | Lemma 4.1 | `907-958` |
| `prop:key` | Proposition 4.2 | `1007` |
| `sec:calculations` | Section 5 | `1172` |
| `lem:raise` | Lemma 5.1 | `1179-1191` |
| `lem:four` | Lemma 5.2 | `1200-1207` |
| `lem:distinct` | Lemma 5.3 | `1212-1221` |
| `lem:correction-calculation` | Lemma 5.4 | `1305` |

Equations (the ones this repository cites): `eq:iso` = (17) at `730`,
`eq:DA` = (18) at `736`, `eq:P` = (19) at `745`, `eq:Hsym` = (20) at `749`,
`eq:sine` = (21) at `753`, `eq:E` = (22) at `765`,
`eq:exact-cancellation` = (23) at `771`, `eq:zero-function` = (24) at `777`,
`eq:construction` = (25) at `785`, `eq:D2a` = (27) and `eq:D2b` = (28) at `829`,
`eq:D1` = (29) at `836`, `eq:line-integral` = (30) at `863`,
`eq:rank-one` = (34) at `971`, `eq:M` = (35) at `983`, `eq:Mcomp` = (36) at `992`,
`eq:B` = (38), `eq:sigma` = (39), `eq:v` = (40) at `1036`,
`eq:ktilde` = (41) at `1046`, `eq:denom` = (43) at `1070`, `eq:beta` = (44) at `1096`,
`eq:raise` = (45) at `1182`, `eq:contract` = (46) at `1195`,
`eq:four` = (47) at `1203`, `eq:distinct` = (48) at `1215`,
`eq:kenergy` = (49) at `1322`, `eq:err` = (50) at `1347`, `eq:onI` = (51) at `1353`,
`eq:support` = (52) at `1362`, `eq:sigmalow` = (53) at `1374`,
`eq:ebound` = (54) at `1395`. The commutation clause of Lemma 5.1 is stated at
`1189-1190` and justified at `1233-1235` (E-010).

| ID | Paper location | Class | Observation | Formalization treatment |
|---|---|---|---|---|
| E-001 | `manuscript.tex:719-740`, `manuscript.tex:822-840`, `manuscript.tex:1179-1219`, `manuscript.tex:1402-1417` | underspecified encoding | The coefficient carrier is described as symmetric functions on tuples, but the precise ordered-tuple measure, quotient, normalization, and distinct-index projection are not defined. These choices are responsible for the `n!` in (17) (`eq:iso`, `manuscript.tex:730`), the rescalings, `Π_n`, and the auxiliary tilde operators. | the recorded convention chooses `n`-element `Finset`s of line indices for the homogeneous Walsh spaces. As amended by the recorded convention, the raw type-`112` correction is still constructed on ordered, possibly coincident row indices and then projected to the Finset carrier; the projection error and Lemma 5.3 remain genuine obligations. Tuple-space factorials and tilde-operator normalizations are not imported: every lowering normalization is derived in the Finset isometry. |
| E-002 | `manuscript.tex:200-225`, `manuscript.tex:555-566`, `manuscript.tex:685-693` | underspecified construction | The continuous-time walk, its exponential semigroup, and its coupling to an independent rate-two Poisson clock are used without a construction identifying those objects. | the recorded convention defines the kernel by Poisson subordination; the displayed generator and occupation identity become lemmas. No continuous-time path process is introduced. |
| E-003 | `manuscript.tex:626-638` | formalization clarification | The argument realifies complex `L²`, invokes an external theorem, and then says the infimum ranges over complex functions. For formalization this must distinguish the complex carrier from its underlying real scalar structure and expose every real part. | the recorded convention proves only the needed upper bound by Lax--Milgram and completing the square; the recorded convention keeps the carrier complex and writes `Re` explicitly. |
| E-004 | `manuscript.tex:711-715` | omitted infrastructure | Completeness of the Walsh products is delegated to conditional expectations and `L²` martingale convergence, without choosing an enumeration/filtration of the countable line-index set or stating the complex-valued convergence result used. | the recorded convention proves density directly: finite-cylinder indicators expand as finite Walsh polynomials, measurable cylinders are measure-dense, and Mathlib's complex `Lp.induction` closes the span. No filtration is introduced. |
| E-005 | `manuscript.tex:965-975` | underspecified domain | The variational expression writes `inf_u` but does not state the measurable/integrable class over which the infimum is taken, so a literal formal statement has no determined domain and may admit junk values. | The estimate lane has a sorry-free candidate predicate `RankOneAdmissible` making the needed measurability and integrability explicit. It remains an unfrozen candidate; a numbered statement ruling is required before it enters the manifest. |
| E-006 | `manuscript.tex:907-917`, `manuscript.tex:1137-1155` | resolved paper mistake | The former Lemma 4.1 quantified over `p₁ = 0`, but then its signed mixed residual was identically zero while part (c) demanded a strictly positive lower bound whenever the logarithmic regime was nonempty. The later application separately has `a = |p₁| > 0`. | the recorded convention records the independent audit, the minimal paper correction `a > 0`, and the live/pinned manuscript update. The v1 anchor is retired; `Manhattan.Frozen.Estimates.lemma_one_coin` v2 is proved by `Manhattan.Estimates.lemmaFourTwoSuccessorClaim_proved`. |
| E-007 | `manuscript.tex:612`, `manuscript.tex:644-660` | Lean transcription defect (paper statement is correct) | The paper fixes `p ∈ 𝕋²`, but version 1 of `Operator.CompetitorBoundClaim` quantified over every raw real representative. Since the concrete fibers are `2π`-periodic and `Operator.maxFrequency` is not, the asserted decay is false at `(2πn,0)` for large `n`. | the recorded convention approves the torus-restricted `prop-frequency` v2 and retires v1 unsealed. The v2 anchor is still a draft because the concrete low-degree Walsh synthesis and energy/cancellation provider described in is absent; no paper edit is warranted. |
| E-008 | `manuscript.tex:1156`; Lean `Manhattan/Glue/Competitor.lean:117` | Lean transcription defect (paper statement is correct) | Version 1 of `Manhattan.Glue.CorrectedLowDegreeCertificate` demanded a horizontal degree-one plus type-`(1,1,2)` competitor at *every* torus frequency. Verbatim, its last clause asks for `∀ (lambda : ℝ), ∀ hlambda : 0 < lambda, lambda ≤ 1 → ∀ p : Fin 2 → ℝ, p 0 ∈ Estimates.torus → p 1 ∈ Estimates.torus → ∃ d : LowDegreeCompetitorData, (concreteFiberEnvironment.dissipativeSkewPair p).hEnergy lambda d.competitor + (concreteFiberEnvironment.dissipativeSkewPair p).hMinusEnergy hlambda (walshL2 ∅ - concreteFiberA p d.competitor) ≤ C * Operator.correctedMajorant r0 lambda p`, while `LowDegreeCompetitorData.competitor` is by construction `b⁻¹ • (degreeOneFrequencySynthesis Axis.horizontal c + type112WalshSynthesis m)`. The paper never asks for this: at `manuscript.tex:1156` it says "If `\|p₂\| > \|p₁\|`, exchange rows and columns", so in the vertical branch the competitor is the axis-swapped one, which is not of that form. The claim is false for every `r0` and `C`: if `sin p₁ = 0` then `⟪1, A_p f⟫ = 0` and `⟪1, A_p k⟫ = 0`, so the residual has constant Walsh component exactly `1` and the Young inequality gives `‖1-A_p g‖²_{-1} ≥ 1/(λ+θ(p))`; taking `s = r₀ e^{-3C/2}/2`, `λ = s²`, `p = (0,s)` forces `L^{3/2} ≤ (3/2)C` while `L^{3/2} ≥ L = 1 + (3/2)C`. Refutation: `Manhattan.Glue.not_correctedLowDegreeCertificate`. | `Manhattan.Glue.CorrectedCompetitorCertificate` replaces it with the Proposition 2.2 form (an arbitrary Walsh competitor `g`, which is what `Operator.CompetitorBoundClaimV2` states) and is discharged by `Manhattan.Glue.correctedCompetitorCertificate_of_sectorEnergy`. The retired definition and its two consumers are kept in place, marked, so the defect is not silently erased; the live chain to Proposition 2.2 never used them. **No paper edit is warranted.** Ruling: the recorded convention (Lean transcription defect, paper correct); same class as E-007. |
| E-009 | `manuscript.tex:791-800` (the substitution `r=p₂+s`, `r'=p₂+s'`, `β=p₁+u`), `manuscript.tex:1040-1063`; Lean `Manhattan/Walsh/Correction.lean:235,460` and `Manhattan/Glue/Correction.lean:170` | Lean transcription defect (paper statement is correct) | The competitor synthesized the manuscript's *shifted-variable* coefficients as `L²` functions of the *unshifted* Walsh line frequency, dropping the compensating unimodular phase. Verbatim: `Manhattan.rawCorrectionFunction kappa q a p₂ x = Estimates.correctionCoefficient kappa q a p₂ (unitTorusAngle (x 0)) (unitTorusAngle (x 1)) (unitTorusAngle (x 2))` with `Manhattan.correctionType112Coefficients hkappa hlambda a p₂ = type112DiagonalProjection (rawCorrectionFourierCoefficients hkappa hlambda a p₂)`, so the Walsh coefficient at the line index `(m,m',n)` was `k̃^(m,m',n)` where the paper needs `e^{i(m+m')p₂+inp₁}k̃^(m,m',n)`; and `Manhattan.Glue.correctedRowFrequency hlambda hcert = realTorusL2 (Estimates.degreeOneCoefficient q p₁) _`, whose frequency function is `g(s)` where the paper needs `g(p₂+s)`. The operator symbols therefore sat at the true frequencies while the coefficients sat at the unshifted ones: `Manhattan.Glue.inner_mixed_walshRaise_axisDegreeOne` produced `i sin(p₂+s)·g(s)` instead of `i sin(r)·g(r)`, and `Manhattan.Glue.type112DStarMixed_eq` produced `-i sin(p₁+u)·∫k̃(s,s',u)ds'` instead of `-i sin(β)·∫k̃(r,r',β)dr'`. Summands 1, 2 and 4 of (22) are upper bounds on weighted energies and were unaffected: the phase is unimodular, so no coefficient modulus moves, and the displaced weight was absorbed by `Manhattan.Glue.dispersion_add_le` (through `Manhattan.Glue.rowWeight_le` and `Manhattan.Glue.symbolWeight_type112_le`). Summand 3 is the one sector that uses a cancellation rather than a bound, and the exact identity `1-σv=Bv` behind the `√L` gain does not survive the two distortion factors `sin(r)/sin(r-p₂)` and `sin(β)/sin(β-p₁)`, the second of which is unbounded near `β=p₁`; `Manhattan.Glue.SummandThreeBound` was therefore not provable for the competitor as defined. Diagnosis:. | The phase is installed, not worked around. `Manhattan.type112ShiftTwist p₁ p₂` multiplies each type-`(1,1,2)` coefficient by `Manhattan.rawType112ShiftPhase p₁ p₂ n = e^{i(m+m')p₂+inp₁}` (`Manhattan.rawType112ShiftPhase_eq`) and `Manhattan.Glue.rowTorusShift p₂` translates the row frequency function by `p₂`; the competitor's two fields are `Manhattan.shiftedCorrectionType112Coefficients` and that translate (`Manhattan/Glue/Correction.lean`). Both maps are isometries, so every energy and norm statement downstream stays true; only proofs changed. Two comparisons became exact identities: summand 1 is now `= Estimates.degreeOneEnergy q (p 0)` (`Manhattan.Glue.hEnergy_correctedRowVector_eq`) instead of `≤ 3 ·` it, and the formalizations frozen momentum `![0,-p₂]` is now reached exactly from the momentum `p` (`Manhattan.Glue.hThreeForm_type112ShiftTwist_frozen`), which discharges `ConcreteHThreeQuadraticBound` at `p` and lowers summand 2 from `14C√L` to `2C√L`. **No paper edit is warranted.** Ruling: the recorded convention (Lean transcription defect, paper correct); same class as E-007 and E-008. Repair report:. |
| E-010 | `manuscript.tex:1233-1235` (justification closing Lemma 5.1), used at `:1299-1302` and `:1402-1406` | underspecification (statement true, stated reason false) | "A multiplier depending on $P$ is a linear combination of simultaneous translations of all line indices." Finite linear combinations of the translations $T_y$ are exactly the trigonometric polynomials in $P$. That covers $H_n=\lambda+2-\cos P_1-\cos P_2$, but NOT the multiplier $M$ of (35), whose terms $2\lvert\sin(P_i/2)\rvert$ are not real-analytic (Fourier coefficients $O(n^{-2})$, all nonzero), so $M$ is not a trigonometric polynomial. $M$ is the multiplier actually fed to (46) in the proofs of Lemma 5.3 and of Lemma 5.4 Step 4. | The CONCLUSION of Lemma 5.1 is true in full generality, proved two independent ways by the the audit independent check: $I-\Pi_n$ is the conditional expectation onto the $\sigma$-algebra generated by $P$ (the paper's own fibre picture at :1276-1279), so it commutes with multiplication by any $P$-measurable function; and $\Pi_n$ lies in the commutant of $\{T_y\}$, whose generated von Neumann algebra is all bounded Borel functions of $P$. Nothing in Lean depends on the paper's stated reason: `Glue.diagonalMultiplierEnergy_le_rawMultiplierEnergy` proves the $M$ case directly and fibrewise. Ruling: the recorded convention, recording the the audit verdict UNDERSPECIFICATION. ERRATA-only per the standing rule, with NO paper edit; the one-sentence amendment is on file in for the author's decision. |
| E-011 | `manuscript.tex:1138-1141`; Lean `Manhattan/Glue/Correction.lean` (`mixedCoefficient`), `Manhattan/Estimates/DegreeThree.lean` (`correctionCoefficient`) | Lean transcription defect (PAPER IS CORRECT) | The manuscript says verbatim: "multiply the coefficient supplied by Proposition~\ref{prop:key} by $\sgn(\sin p_1)$; call the result $k_p$". The Lean competitor omitted that multiplier: `correctionCoefficient`, `correctionType112Coefficients`, `shiftedCorrectionType112Coefficients` and `correctedLowDegreeData` contained no `Real.sign`. Consequence, machine-checked (`norm_mixedResidual_le_norm_sub_correction_of_sin_neg`, `mixedResidual_sub_correction_of_sin_neg`): when `sin p_1 < 0` the two mixed components ADD rather than cancelling, so the mixed sector energy is at least the uncorrected Lemma 4.1(c) residual, of size `L`. `SummandThreeBound` was therefore FALSE as stated, not merely unproved. | Repaired (commit `f2e110e`): `mixedCoefficient := (Real.sign (Real.sin (p 0)) : C) • shiftedCorrectionType112Coefficients ...`. Safe for the other summands because the factor is unimodular on the live regime: norms and energy bounds on `k_p` are unchanged, `type112DStarTwoRow` stays zero by linearity (`type112DStarTwoRow_smul`), summand 4 is provably sign-blind, and summand 2 discharges the sign via `norm_sgn_sq_le_one`. Ruling the recorded convention. HAZARD recorded by the audit finding 11: `Real.sign 0 = 0`, so this is safe only because `hnormalization : degreeOneNormalization q (p 0) != 0` forces `sin p_1 != 0`; a caller dropping that hypothesis would silently zero the whole degree-three part while every energy bound stayed trivially true. A guard lemma `hnormalization -> sin (p 0) != 0` is wanted. |
